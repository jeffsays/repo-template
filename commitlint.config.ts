/**
 * @file - Commitlint configuration
 * @author - Jeff Beck
 */

import type { UserConfig } from "@commitlint/types";

const Configuration: UserConfig = {
  extends: ["@jeffsays/commitlint"],
};

export default Configuration;
